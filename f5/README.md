# F5 BIG-IP / ASM → Microsoft Sentinel

> Coming soon. Will cover CEF forwarding from F5 BIG-IP LTM and ASM (WAF) into Sentinel via CommonSecurityLog, with ASIM mapping to `_Im_NetworkSession` and `_Im_WebSession`.
