# Palo Alto PAN-OS → Microsoft Sentinel

> Coming soon. Will cover CEF/syslog forwarding from PAN-OS into Sentinel via CommonSecurityLog, with ASIM mapping to `_Im_NetworkSession` and `_Im_WebSession`.
